

- Command class with call backs

- Make a receiver class, which has a fdReader in it.

- Make the sender class have a fdReader in it.

- When start up occurs, the user needs to broadcast their id so they can get
their immediate subscriptions

- Sort messages in a channel by timestamp

TODO by the end of today
Send a watch (channels) (integer)
Send a message
Receive a message
