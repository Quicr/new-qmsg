#include "Parser.hh"

Parser::Parser()
{

}

Parser::~Parser()
{

}

bool Parser::Parse(char* buffer, const unsigned int buffer_length, int &command)
{
    // TODO strip off characters that cannot be sent ex. CR & LF

    // TODO read the input and see if there was a command given.

    // determine the command
    // command = Command::;
    return true;
}